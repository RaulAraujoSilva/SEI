import React from 'react';
import { Box, Typography } from '@mui/material';

const LLMDashboard: React.FC = () => {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        Dashboard LLM
      </Typography>
      <Typography>
        Dashboard de análises LLM será implementado aqui.
      </Typography>
    </Box>
  );
};

export default LLMDashboard; 